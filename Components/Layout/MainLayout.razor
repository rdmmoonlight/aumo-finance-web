@inherits LayoutComponentBase
@using Microsoft.AspNetCore.Components.Authorization

<!-- ==========================================
     Aumo Finance - Main Layout
     Cosmic / Nebula Theme
     ========================================== -->

<div class="d-flex flex-column min-vh-100 min-vw-0 aumo-main-shell">

    <!-- TopBar (Dipanggil dari folder Layout/Components) -->
    <AuthorizeView>
        <Authorized>
            <AumoFinance.Components.Layout.TopBar />
        </Authorized>
    </AuthorizeView>

    <!-- Shell Utama: Sidebar & Main Content -->
    <div class="d-flex flex-grow-1 overflow-hidden">
        
        <!-- SIDEBAR CONTAINER (Memanggil Sidebar.razor) -->
        <AumoFinance.Components.Layout.Sidebar />

        <!-- MAIN CONTENT AREA -->
        <main id="mainContent" class="content flex-grow-1 p-2 p-md-4 overflow-auto d-flex flex-column" role="main">
            @Body
        </main>

    </div>

</div>

<style>
    body {
        transition: background-color 0.3s ease, color 0.3s ease;
    }

    .content { 
        transition: background-color 0.3s ease;
    }

    .fs-7 { font-size: 0.825rem; }
    .fs-8 { font-size: 0.725rem; }

    @@media (max-width: 767.98px) {
        #mainContent {
            padding: 0.75rem 0.5rem !important;
        }
    }
</style>
