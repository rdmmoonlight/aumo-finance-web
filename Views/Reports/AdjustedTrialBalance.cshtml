@using System.Security.Claims
@{
    ViewData["Title"] = "Adjusted Trial Balance";
    var userId = Guid.Parse(User.FindFirstValue(ClaimTypes.NameIdentifier)!);
}

<component type="typeof(AumoFinance.Components.TrialBalancePage)" render-mode="ServerPrerendered"
           param-UserId="@userId" param-IncludeAdjusting="true" />
